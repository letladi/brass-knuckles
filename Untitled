function find(list, key) {
  let lo = 0, hi = list.length
  while (hi > lo + 1) {
    const mid = Math.floor((hi + lo) / 2)
    if (key < list[mid])
      hi = mid
    else
      lo = mid
  }
  return lo
}
